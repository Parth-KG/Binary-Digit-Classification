# Binary-Digit-Classification
A 2-layer neural network built from scratch using NumPy that classifies handwritten digits (0 vs 1) from MNIST. Implements forward propagation, backpropagation, and gradient descent manually — no PyTorch or TensorFlow.
